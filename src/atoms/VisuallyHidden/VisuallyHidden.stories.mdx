import { Meta, Props, Source } from '@storybook/addon-docs/blocks'
import { VisuallyHidden } from '.'

<Meta title="Atoms/VisuallyHidden" component={VisuallyHidden} />

# VisuallyHidden

The `VisuallyHidden`

<Source dark code={`
  <VisuallyHidden>
    <p>Children</p>
  </VisuallyHidden>
`} />

<Props of={VisuallyHidden} />
