import { Meta, Preview, Props, Source, Story } from '@storybook/addon-docs/blocks'
import { PageContainer } from '.'
import { ThemeProvider } from '../../providers'
import { default as ContentContainer } from '../ContentContainer'
import theme from '../../__mocks__/theme'

<Meta title="Components/PageContainer" component={PageContainer} />

# PageContainer

The `PageContainer` flexes to fill the unused space within its parent

<Source dark code={`
  <PageContainer header={<Component />} footer={<Component />}>
    {children}
  </PageContainer>
`} />

<Props of={PageContainer} />

## With theme

<Preview withToolbar>
  <Story name="With theme">
    <ThemeProvider theme={theme}>
      <div style={{ display: 'flex', flexDirection: 'column', height: 300 }}>
        <PageContainer
          as="main"
          header={<ContentContainer as="header">Header</ContentContainer>}
          footer={<ContentContainer as="footer">Footer</ContentContainer>}
        >
          Content
        </PageContainer>
      </div>
    </ThemeProvider>
  </Story>
</Preview>
