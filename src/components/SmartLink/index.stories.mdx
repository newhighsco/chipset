import { Meta, Props, Source } from '@storybook/addon-docs/blocks'
import { SmartLink } from '.'

<Meta title="Components/SmartLink" component={SmartLink} />

# SmartLink

Use `SmartLink` to automatically render an button, internal anchor, or outbound link

<Source dark code={`
  <SmartLink>
    {children}
  </SmartLink>
`} />

<Props of={SmartLink} />
