import { Fragment } from 'react'
import { Meta, Preview, Props, Source, Story } from '@storybook/addon-docs/blocks'
import { ContentContainer } from '.'
import { ThemeProvider } from '../../providers'
import theme from '../__mocks__/theme'

<Meta title="Components/ContentContainer" component={ContentContainer} />

# ContentContainer

The `ContentContainer` contrains the width of an item within its parent

<Source dark code={`
  <ContentContainer>
    {children}
  </ContentContainer>
`} />

<Props of={ContentContainer} />

## With theme

<Preview>
  <Story name="With theme">
    <ThemeProvider theme={theme}>
      <Fragment>
        <ContentContainer>Content</ContentContainer>
        <ContentContainer gutter>Content with gutter</ContentContainer>
        <ContentContainer fullWidth>Full-width content</ContentContainer>
        <ContentContainer fullWidth gutter>Full-width content with gutter</ContentContainer>
      </Fragment>
    </ThemeProvider>
  </Story>
</Preview>
