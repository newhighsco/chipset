import { Meta, Preview, Props } from '@storybook/addon-docs/blocks'
import { ContentContainer } from '.'

<Meta title="Core/ContentContainer" component={ContentContainer} />

# ContentContainer

The `ContentContainer`

<Preview isExpanded>
  <ContentContainer>
    <p>Children</p>
  </ContentContainer>
</Preview>

<Props of={ContentContainer} />
